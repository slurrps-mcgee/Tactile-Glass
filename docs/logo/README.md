# TactileGlass logo

- `tactileglass-mark.svg` / `.png` — TG mark (overlapping neu + glass tiles)
- `tactileglass.svg` — nav lockup (mark + wordmark)

Use the SVG lockup in the docs topnav. Prefer SVG over PNG for crisp UI.
